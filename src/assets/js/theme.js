//
// theme.js
// Theme JavaScript
//

/**
 * Listen to scroll to change header opacity class
 */
/*function checkScroll(){
    var startY = $('.navbar-fixed-on-scroll').height() * 2; //The point where the navbar changes in px


console.log("11111111111");


    if ($('.navbar').hasClass('cambiar-color') ){
    	//console.log("entraa");
		if($(window).scrollTop() > startY){
	        $('.navbar').addClass("bg-white fixed-top border-bottom navbar-light ");
	        $('.navbar').removeClass("navbar-dark");
	    }else{
	        $('.navbar').removeClass("bg-white fixed-top border-bottom navbar-light ");
	        $('.navbar').addClass("navbar-dark");
	    }	

   	//sino cambia el fondo
    }else{
	    if($(window).scrollTop() > startY){
	        $('.navbar').addClass("bg-white fixed-top border-bottom");
	    }else{
	        $('.navbar').removeClass("bg-white fixed-top border-bottom");
	    }	
    }


    
}

if($('.navbar-fixed-on-scroll').length > 0){
    $(window).on("scroll load resize", function(){
        checkScroll();
    });
}*/